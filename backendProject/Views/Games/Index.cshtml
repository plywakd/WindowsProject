@model IEnumerable<backendProject.Models.Game>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.gameName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.wordCount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.minWordspeed)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.maxMistakes)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.difficulty)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.gameName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.wordCount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.minWordspeed)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.maxMistakes)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.difficulty)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
